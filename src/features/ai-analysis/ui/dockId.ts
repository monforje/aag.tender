/** Адрес панели в DOM — ОТДЕЛЬНЫМ МОДУЛЕМ, и это не педантизм.
 *
 *  Константу читают трое: сама панель разбора, заглушка чата и триггер на
 *  странице (`aria-controls`). Пока она лежала в `AiDock.tsx`, любой из них
 *  тянул за ней ВЕСЬ чат — ответчик, историю, стриминг, правила подсказок, —
 *  и `lazy()` на заглушке не давал ничего: статический импорт одного
 *  идентификатора возвращал модуль в главный чанк.
 *
 *  Здесь нет и не должно быть ничего, кроме строки: как только тут появится
 *  импорт, ленивая граница снова протечёт. */
export const AI_DOCK_ID = 'ai-analysis-dock';
