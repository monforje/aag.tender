/** Публичная точка слайса «Сравнение КП». Внутрь не заходить: сегменты
 *  переставляются между собой (счёт уже переезжал из одного файла в шесть), и
 *  каждый глубокий импорт превратил бы такую перестановку в правку двадцати
 *  файлов.
 *
 *  Реэкспорт — `export *`, а не перечень имён. Перечень пришлось бы править
 *  дважды на каждую новую функцию, и раз за разом это забывали: слайс
 *  разрастался, а наружу торчало старое. Коллизий между сегментами нет — они
 *  и делятся по смыслу, а не по алфавиту. */

/* Контракт данных и прямые обращения к нему. */
export * from './model/contract';
/* Пороги аналитики — настройки тендера. */
export * from './model/thresholds';
/* Счёт: итоги, ранжир, производные числа строки. */
export * from './model/calc';
/* Предикаты среза. */
export * from './model/filters';
/* Оси состояния экрана. */
export * from './model/view';
/* Матрица условий поставщиков: сборка строк из ответов формы. */
export * from './model/terms';
/* Деньги и числа одним форматом на весь экран. */
export * from './lib/format';
/* Дверь к данным. Фикстуры за ней НЕ реэкспортируются намеренно: экран,
   импортирующий мок по имени, переживёт подключение сервера и продолжит
   молча показывать демо-цифры. */
export * from './api/comparison.api';
