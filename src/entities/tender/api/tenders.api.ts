/** ЕДИНСТВЕННАЯ ДВЕРЬ К СТРОКАМ РЕЕСТРА — та же роль, что у
 *  `comparison.api.ts` в соседнем слайсе, и те же причины: фикстура
 *  упоминается только здесь, а функции асинхронны уже сейчас, чтобы у экрана
 *  были состояния загрузки и ошибки ДО того, как появится сеть.
 *
 *  Отбор и сортировка сегодня целиком на клиенте (`applyFilters`) — строк
 *  девять. Когда их станет тысяча, фильтр уедет в параметры запроса, и это
 *  будет правка этого файла плюс типа `Filters`, который уже описывает ровно
 *  то, что уйдёт в query. */

import type { TenderRow } from '../model/tender';
import { ROWS } from './tenders.mock';

export async function fetchTenders(): Promise<TenderRow[]> {
  return ROWS;
}

/** Тендер по номеру. `null`, а не бросок: неизвестный номер — это опечатка в
 *  адресе или мёртвая ссылка, штатный случай, на который у карточки есть
 *  ответ (возврат в реестр), а не сбой. */
export async function fetchTender(id: string | undefined): Promise<TenderRow | null> {
  return ROWS.find((row) => row.id === id) ?? null;
}
