/** Публичная точка слайса «Тендер» (реестр). Сравнение КП живёт отдельным
 *  слайсом — `@/entities/comparison`: у него другая жизнь и другие
 *  потребители, а полторы тысячи строк счёта за одной точкой с реестром
 *  заставляли редактировать общий barrel на каждую правку любой из половин.
 *
 *  Реэкспорт — `export *`: перечень имён здесь уже разъезжался с содержимым
 *  слайса. */

/* Запись реестра, словарь статусов, срок сбора КП. */
export * from './model/tender';
/* Срез реестра: набор фильтров, счётчики, применение. */
export * from './model/filters';
/* Дверь к данным. Фикстура (`tenders.mock.ts`) наружу не идёт намеренно. */
export * from './api/tenders.api';
