export { Input, type InputProps } from './Input';
export { Textarea, type TextareaProps } from './Textarea';

/* Поля ввода: одна поверхность на оба (модуль общий, как у Dropdown его
   меню). Поверхность повторяет вторичную кнопку (32px / радиус 6 /
   хэйрлайн-тень), чтобы полоса «поле + кнопка» была одной высоты; правила
   продублированы в этом модуле, а не импортированы из Button.module.css —
   селектор CSS-модуля не пересекает границу модуля, и «общий» класс между
   двумя слайсами молча не совпал бы ни с чем (дважды поймано на этом). */
