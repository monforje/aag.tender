export { ButtonGroup } from './ButtonGroup';

/* Ряд связанных кнопок. Отдельный слайс, а не проп у <Button>: группа —
   свойство РЯДА, а не кнопки, и смешивать их значило бы научить кнопку
   знать про соседей. */
