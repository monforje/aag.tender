export { ConfirmDialog, type ConfirmDialogProps } from './ConfirmDialog';

/* Паттерн поверх <Modal>: анатомия «вопрос → пояснение → Отмена | Действие».
   Вынесен в shared, потому что повторяется у любого CRUD-экрана, а не только
   у тендеров; знание о том, ЧТО удаляется, остаётся на странице. */
