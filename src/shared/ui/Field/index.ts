export { Field, useFieldControl, type FieldControl } from './Field';

/* Поле как композиция «подпись + контрол + подсказка», а не Input с пропом
   label: между подписью и контролом регулярно встают свои узлы (иконки,
   суффиксы, группы), и проп-обёртка пришлось бы обходить.

   Связь с контролом — через контекст (useFieldControl): Input, Textarea и
   Select берут из него id и aria-describedby ТОЛЬКО когда их не задали явно.
   Это единственный способ сделать «контрол без пропов связки» честным:
   клонирование детей ломает memo и ref'ы, а require-id-руками забывают. */
