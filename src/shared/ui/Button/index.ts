export { Button, type ButtonProps } from './Button';

/* Вариант danger (необратимое разрушающее действие) добавлен вместе с
   <ConfirmDialog>: тон берётся из лестницы --cu-tone-*, своей палитры у
   кнопки нет. */
