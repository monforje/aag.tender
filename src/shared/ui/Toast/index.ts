export { ToastProvider, useToast, type ToastInput } from './Toast';

/* Тосты живут в провайдере, а не в сторе Zustand сознательно: это
   презентационная очередь с таймерами, а не состояние домена. Провайдер
   монтируется один раз у корня приложения (app/index.tsx). */
