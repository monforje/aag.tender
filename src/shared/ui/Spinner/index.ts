export { Spinner, type SpinnerProps } from './Spinner';

/* Крутящееся кольцо — для процессов БЕЗ места на экране (отправка кнопки,
   догрузка счётчика). Там, где известно, ЧТО появится, — <Skeleton>. */
