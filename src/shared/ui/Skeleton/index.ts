export { Skeleton, type SkeletonProps } from './Skeleton';

/* Заглушка формы, а не состояния: «грузится» — единственное, что она умеет
   говорить. Всё остальное («пусто», «сломалось») — слова других компонентов. */
