export { ErrorState, type ErrorStateProps } from './ErrorState';

/* Тонкая обёртка НАД EmptyState, а не второй заглушкой: у ошибки та же
   анатомия (глиф, заголовок, описание, действие), другой тон и другой
   единственный выход. Второй самостоятельный компонент дал бы две формы
   одного события. */
