export { Pagination, type PaginationProps } from './Pagination';

/* Пагинация сама исчезает, когда страниц нет (pageCount <= 1): условие
   «а надо ли её показывать» — часть компонента, а не забота каждой
   страницы-потребителя. */
