export { Text, Heading, type TextProps, type TextSize, type TextTone, type HeadingProps } from './Typography';

/* Типографика как примитив: <Text> и <Heading>. Пара, а не один компонент,
   потому что вопрос «строка это или заголовок» — семантический, и решаться
   он должен выбором компонента, а не тегом руками.

   Оба намеренно НЕ умеют margin: вертикальный ритм держит раскладка
   (<Stack>, шапки поверхностей). Компонент, который добавляет поля,
   становится сюрпризом внутри чужого flex. */
