export { Alert, type AlertProps } from './Alert';

/* Тона и их глифы по умолчанию — из общего словаря проекта (--cu-tone-*,
   таблица TONE у Badge): алерт не заводит своей палитры и не выбирает
   цвета на месте потребителя. */
