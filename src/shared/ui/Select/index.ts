export { Select, type SelectProps, type SelectOption } from './Select';

/* Селект построен ПОВЕРХ меню проекта (Dropdown + MenuItem), а не как второй
   оверлей: клавиатура, Escape, группы и роли уже сделаны там, и вторая
   механика означала бы два места правки одного поведения.

   Триггер повторяет поверхность поля из Input.module.css дословно: в форме
   селект обязан читаться полем. Дублирование сознательное — селектор
   CSS-модуля границу слайса не пересекает. */
