export { Drawer, drawerPart, type DrawerProps } from './Drawer';

/* Боковая панель на нативном <dialog> — сестра <Modal>: открытость проп,
   закрытие одним источником (close-событие), подложка от платформы.
   Отдельный слайс, потому что у дровера другая геометрия и своё движение:
   панель ВЪЕЗЖАЕТ с края, окно не двигается вовсе. */
