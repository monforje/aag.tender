import { Icon } from '@/shared/ui/Icon';
import s from './Sidebar.module.css';

/**
 * Подвал панели: настройка состава сайдбара.
 *
 * КОГДА:  действие над самим инструментом, а не над данными — «настроить, что
 *         вы видите». Такому место внизу: им пользуются один раз и надолго.
 * НЕ ДЛЯ: частых действий и создания сущностей — они наверху, у заголовка.
 *
 * UX:     единственная в панели кнопка с заливкой в покое (--cu-fill-subtle):
 *         она не должна конкурировать с деревом, но и теряться не должна,
 *         потому что ничем другим не выделена — ни цветом, ни позицией.
 *         Из-за собственной заливки hover идёт ступенью выше (рецепт 1).
 *         Подвал не прокручивается вместе со списком: настройки должны быть
 *         доступны, не листая дерево до конца.
 * A11Y:   обычная кнопка с текстовой подписью, отдельный label не нужен.
 *
 * @example
 * <SidebarFooter />
 */
export function SidebarFooter() {
  return (
    <div className={s.sidebarFinalFooter}>
      <button className={s.sidebarFinalCustomize} type="button">
        <Icon name="sliders" /> Customize Sidebar
      </button>
    </div>
  );
}
