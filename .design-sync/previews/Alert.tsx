import { Alert, Stack } from 'clickup-shell';

/** Четыре тона: глиф по умолчанию свой у каждого, заголовок опционален. */
export const Tones = () => (
  <Stack gap={3}>
    <Alert tone="info" title="Срок сбора продлён">Новая дата окончания — 30.09.2026. Подрядчики уведомлены.</Alert>
    <Alert tone="success" title="КП отправлено">Предложение ушло заказчику, копия — в журнале тендера.</Alert>
    <Alert tone="warning" title="Одно КП заполнено частично">По трём позициям из сорока двух цены не проставлены.</Alert>
    <Alert tone="danger" title="Тендер отменён">Заказчик отозвал закупку 14.06.2026. Работа по КП остановлена.</Alert>
  </Stack>
);

/** Без заголовка — одна строка в потоке страницы. */
export const Compact = () => (
  <Stack gap={3}>
    <Alert tone="info">Черновик сохраняется автоматически.</Alert>
    <Alert tone="neutral">Фильтры в ссылку не попадают — F5 их сбросит.</Alert>
  </Stack>
);

/** Крестик появляется только у гасимого сообщения. */
export const Dismissible = () => (
  <Alert tone="warning" title="Разброс цен выше порога" onClose={() => {}}>
    По позиции «Устройство пароизоляции» разброс 24% — проверьте объём в смете.
  </Alert>
);
