import { PageHeader, PageTitle, Screen } from 'clickup-shell';
/** Заголовок ЭКРАНА — один на страницу, стоит на колонке контента (12px). */
export const Basic = () => (
  <Screen><PageHeader><PageTitle>Реестр тендеров</PageTitle></PageHeader></Screen>
);
