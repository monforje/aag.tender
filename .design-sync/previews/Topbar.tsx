import { Topbar } from 'clickup-shell';
/** Топбар каркаса: слоты left/center/right целиком. */
export const Bar = () => (
  <div style={{ width: 900, background: 'rgb(38,38,38)' }}><Topbar /></div>
);
