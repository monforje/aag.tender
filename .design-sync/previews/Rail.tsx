import { Rail } from 'clickup-shell';
/** Иконный рейл: разделы приложения. */
export const Sections = () => (
  <div style={{ height: 420, display: 'flex', background: 'rgb(38,38,38)', width: 64 }}><Rail /></div>
);
