import { TenderRegistryPage } from 'clickup-shell';
/** Экран реестра целиком: вкладки среза, полоса фильтров, таблица. */
export const Screen = () => (
  <div style={{ width: 940 }}><TenderRegistryPage /></div>
);
