import { ErrorState } from 'clickup-shell';

/** Запрос не удался: та же форма, тон danger и «Повторить». */
export const LoadFailed = () => (
  <ErrorState
    title="Не удалось загрузить сравнение КП"
    description="Сервер не ответил за 30 секунд. Данные не потеряны — попробуйте ещё раз."
    onRetry={() => {}}
  />
);

/** «Нет доступа» — не ошибка загрузки: кнопки «Повторить» тут быть не должно. */
export const NoAccess = () => (
  <ErrorState
    title="Нет доступа к этому тендеру"
    description="Карточка закреплена за другим подразделением. Запросите доступ у ответственного."
  />
);
